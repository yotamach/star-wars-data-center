type Category = { name: string, url: string };

interface Person {
    name: string,
    height: string,
    mass: string,
    gender: string,
    birth_year: string
}

export {
    Person,
    Category,
}